/**
 * Types Index
 * 
 * Re-exports type definitions.
 * Note: Main exports happen through src/index.ts to avoid conflicts
 */

// Types are exported individually from src/index.ts to avoid naming conflicts
// Import directly from specific files when needed:
// import type { ApiErrorResponse } from '@reech/shared/types/api';
// import type { BaseEntity } from '@reech/shared/types/database';
// import type { AppConfig } from '@reech/shared/types/config';
